<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sharinghands">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoUpload"></activity>
        <activity android:name=".ui.NGO.ActivePosts" />
        <activity
            android:name=".ui.NGO.Dashboard"
            android:label="@string/title_activity_dashboard" />
        <activity android:name=".CreatePost" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".DonorHome" />
        <activity android:name=".ui.Student.StudentRegistration" />
        <activity android:name=".ui.NGO.NGORegistration" />
        <activity android:name=".ui.Donor.DonorRegistration" />
        <activity android:name=".SinglePost" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>